function App(){
    return(
        <main>
            <p>Had to comment out the Route paths.</p>
        </main>
    )
}

export default App

// Had to comment out the Route paths below


/*
import React from 'react';
import {
    Routes, Route,
} from 'react-router-dom';

function App() {
    <Routes>
        <Route path={'/'} element={<MyMain/>}/>
        <Route path={"/1"} element={<NasaClass />}/>
        <Route path={"/2"} element={<GridNumbers />}/>
        <Route path={"/3"} element={<ColorChanger />}/>
        <Route path={"/4"} element={<DataPlot />}/>
    </Routes>
}
export default App*/
